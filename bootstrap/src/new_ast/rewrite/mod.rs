use crate::new_ast::ast::SourceFile;
use crate::new_ast::parse::node::RootNode;

pub(crate) struct Rewriter {}

impl Rewriter {
    pub(crate) fn rewrite(node: RootNode) -> crate::new_ast::Result<SourceFile> {
        todo!()
    }
}